# Legacy Static Demo Archive

This directory holds repo material from the older static itinerary demo that is no longer part of the active product direction.

These files are kept only for historical reference. They should not be treated as current design guidance or as canonical implementation inputs for new planner work.

Archived here:

- generated HTML artifacts for the older static bundle demo
- one-off setup notes tied to that older deployment flow
- scratch or generated files that were checked in during that phase
- duplicate test material from the pre-standardized test layout

Current implementation work should instead follow:

- `docs/implementation-plan.md`
- `docs/product-architecture-brief.md`
- `docs/leisure-preference-contract.md`
